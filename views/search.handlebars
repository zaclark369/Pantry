<div class="container pt-3">
    <div class="row">
        <div id="ingredients" class="col-6 col-md-4">
            {{> ingredients}}
        </div>

        {{#if loggedIn}}
        <div id="recipe-list" class="col-md-8 d-flex flex-wrap">
            {{> recipe-list}}
        </div>
        {{else}}
        <div id="recipe-list" class="col-md-8 d-flex flex-wrap">
            {{> recipe-list-not-loggedIn}}
        </div>
        {{/if}}
    </div>
</div>
